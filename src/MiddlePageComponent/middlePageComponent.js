import React from 'react';

const MiddlePageComponent = props => {
    return (
        <React.Fragment>
            { props.children }
        </React.Fragment>
    )
}

export default MiddlePageComponent